# Внутренние заметки (не для заказчика)

Сюда — наши допущения и контекст.  
**Ему отдаём только:** `README.md`, `HANDOFF.md`, `docs/MAPS.md` (без этой папки `internal/`).

---

## Что мы реально не знаем

Код его программы / diocese-API не смотрели. Всё ниже — рабочие гипотезы по макету ТЗ и нашему опыту с храмами ККЕР.

Не писать ему формулировки вроде «мы не видели», «не знаем наверняка», «парнишка» и т.п.

---

## Гипотезы (для нас)

| # | Суть | Зачем в проф. доке |
|---|------|-------------------|
| A | У него уже база приходов → нужна тонкая Python/Django-обёртка | §4 схема A в MAPS |
| B | Тот же API, что у приложения ЯКатолик | схема B |
| C | У него Яндекс MapKit, точки свои — нам только JSON export | схема C |
| D | Данных/API ещё нет — пока demo/auto | схема D |

Почему A наиболее вероятна: в макете явно «карта из diocese-API», в футере ТЗ — Django + Next; наш портал — статика, ему проще отдать JSON.

---

## Наш бэкенд vs его

- Архив + чат + RAG сейчас на нашем прокси (`ARCHIVE_API_BASE`).
- Карта: адаптер готов; live может бить в наш `/api/temples/search` или сразу в его URL.
- При передаче папки `Ave_Maria` — клиентские доки без упоминания «Вера и Разум» как внутренней кухни (в MAPS контракт описан нейтрально).

---

## Аудио / видео / радио

Это **наши** экраны (Вера и Разум). Из навигации ЯКатолик убраны.  
Файлы `video.html` / `audio.html` / `radio.html` могут остаться в репо, но не в меню.

Навигация портала для них: Главная · Новости · Статьи · Карта · Спросить (+ Библиотека в desktop).

## Что не коммитить ему в корень как «для вас»

- `docs/internal/*` — только наши  
- Черновики, agent transcripts, ссылки на Railway/личные прокси — по желанию вычистить из README перед zip  
